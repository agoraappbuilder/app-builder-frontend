{
  "name": "agora-app-builder",
  "version": "1.0.0",
 "scripts": {
    "start": "agora-app-builder-cli",
    "ci": "npm install && agora-app-builder-cli install  && agora-app-builder-cli build web --prod &&  cp -r Builds/web testappbuilder/dist && cp testappbuilder/vercel.json testappbuilder/dist"
  },
  "keywords": [],
  "license": "MIT",
  "dependencies": {
    "agora-app-builder-cli": "2.0.9"
  }
}